[package]
name = "s3post.rs"
version = "0.1.0"
authors = ["Joseph Kordish <joe@unicornclouds.com>"]

description = "Post gzip compressed logs to S3"
homepage = "https://github.com/jkordish/s3post.rs"
repository = "https://github.com/jkordish/s3post.rs"

readme = "README.md"
license = "The MIT License (MIT)"

keywords = [
  "aws",
  "logs"
]

[[bin]]
name = "s3post"

[dependencies]
syslog = "^3"
flate2 = { version = "^0.2", features = ["miniz-sys"], default-features = false  }
crossbeam = "^0.2"
chrono = "^0.3"
ifaces = "^0.1"
walkdir = "^1"
serde_json = "^1"
serde = "^1"
serde_derive = "^1"
rusoto = {version = "^0.24", default-features = false, features = ["s3", "sts"]}
clippy = {version="*", optional = true}

[features]
default=[]

[profile.release]
panic = "abort"
